



fn foo(bar: fn(int, float)void, arg: int, arg2: float) -> int {
    bar(arg, arg2);
    return 3;
}

fn internal(x: int, y: float) -> void {
    print "x", x;
    print "y", y;
}

print(foo(internal, 3, 4.0));

const x = 42;
fn bar() {
    print x;
}
bar();