namespace A {
  namespace B {
    var x = 0;
  }
}

namespace C {
  namespace A {
    namespace B {
      var x = 1;
    }
  }
  fn print_x() {
    // check for local ns A::B::x 
    // before global
    print A::B::x;
  }
}

C::print_x();