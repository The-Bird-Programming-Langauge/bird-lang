namespace Math {
    var x: int = 0;
    const arr = [ 1, 2, 3 ];

    print 1;
    
    struct Person {
        name: str,
        age: int
    };
    namespace Trig {
        fn sin(x: float) -> float {
            return 0.7071;
        }
    }
}