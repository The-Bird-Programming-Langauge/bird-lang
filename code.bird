fn foobar(bar: (int)void) -> (int)void {
        return bar;
}
var x = foobar(fn(x: int) -> void { print x; });
x(3);

struct Foo {
        val: int;         
        fn foo() -> void { return self; }
};         

const bar: Foo = Foo {};bar.foo();