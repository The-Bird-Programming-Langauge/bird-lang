namespace Math {
    var x: str = "math rules!";
    var y: int = 42;
    var z: float = 3.14;
    const arr = [ 1, 2, 3 ];

    struct point {
        x: float,
        y: float
    };
    namespace Trig {
        var w: float = 1.41;
        fn sin() -> float {
            return 0.7071;
        }
    }
}

print Math::x;
print Math::y;
print Math::z;

print Math::Trig::w;

print Math::arr[0];
print Math::arr[1];
print Math::arr[2];