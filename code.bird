var arr = [ 1, 2, 3 ];
for x in iter(arr) {
    print x;
}

for x in iter([ 1, 2, 3 ]) {
    print x;
}

var s = "hi";
for x in iter(s) {
    print x;
}

for x in iter("bye") {
    print x;
}

var str_arr = [ "hello", "goodbye" ];
for x in iter(str_arr) {
    print x;
}

for x in iter([ "hello", "goodbye" ]) {
    print x;
}
