
fn foobar(end: int) -> int[]  {
    const foo: int[] =  [1,2,3,4];
    for var i = 0; i < end; i += 1 {
        'c' as str;
    }
    return foo;
}

const foo = foobar(100000);

print length(foo);
for var i = 0; i < length(foo); i += 1
{
    print foo[i];
}
