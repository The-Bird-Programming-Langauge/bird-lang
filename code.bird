
fn checkNum(x: int) -> void {
        if (x == 0) {
                print "zero";
        } else if (x > 0) {
                print "positive";
        } else {
                print "negative";
        }
}

print checkNum(1);
print checkNum(-1);