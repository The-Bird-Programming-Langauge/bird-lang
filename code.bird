struct Data {
    stuff: int;
};

var x = Data { stuff = 1};
const data: Data[] = [x];

for var i = 0; i < 10; i += 1 {
    var x = Data {
        stuff = i,
    };
    push(data, x);
}


for var i = 0; i < length(data); i += 1 {
    print data[i].stuff;
}