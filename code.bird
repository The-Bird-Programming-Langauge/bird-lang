type years = int;
struct Person {
    age: years,
};

var person: Person = Person {
    age = 1,
};

print person.age;

person.age = 2;

print person.age;

type name = str;
var foo: name = "marci" as name;

print foo;

foo = "klaus" as name;

print foo;